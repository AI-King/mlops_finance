global:
  # Prometheus samples targets every 15 seconds.
  scrape_interval: 15s
scrape_configs:
  - job_name: fraud-api
    # Docker Compose resolves the api service name on its private network.
    static_configs:
      - targets: ["api:8000"]
