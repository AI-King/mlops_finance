apiVersion: 1

providers:
  - name: MLOps Dashboards
    # Grafana loads dashboard JSON files from this directory at startup.
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning-dashboard-json
